# OmniGemLevel BuiltByBit SEO Listing

## Recommended Resource Name

**OmniGemLevel | MMOItems Gem Upgrade & Fusion Addon**

Why this name:

- Starts with the unique product name.
- Includes the high-intent keyword `MMOItems`.
- Explains the two searchable outcomes: gem upgrade and gem fusion.
- Avoids unsupported claims such as Folia support or universal Minecraft versions.

Alternative titles:

1. `OmniGemLevel | Smart Gem Leveling for MMOItems`
2. `OmniGemLevel | Duplicate Gem Fusion & Socket Upgrades`
3. `OmniGemLevel | MMOItems Gemstone Progression Addon`

## Listing Fields

| Field | Recommended value |
|---|---|
| Resource title | `OmniGemLevel | MMOItems Gem Upgrade & Fusion Addon` |
| URL slug | `omnigemlevel-mmoitems-gem-upgrade-addon` |
| Category | Minecraft plugins |
| Author | `SalyVn` |
| Current version | `1.0.0` |
| Platform | Paper |
| Supported Minecraft version | `1.21` |
| Required dependencies | MMOItems, MythicLib |
| Main configuration type | YAML |
| Documentation | `https://salyys1.github.io/OmniGemLevel/` |

## Short Description

Merge same-id, same-level MMOItems gemstones into the next level without using another socket. Includes configurable caps and safe native fallback.

## Search Snippet

OmniGemLevel is a focused MMOItems gemstone progression addon for Paper 1.21. Exact duplicate gems upgrade the existing socket to the next configured level, while different ids and levels remain in MMOItems native handling.

## Keyword Map

Primary keywords:

- `mmoitems gem upgrade`
- `mmoitems gemstone addon`
- `minecraft gem fusion plugin`

Secondary keywords:

- `mmoitems socket upgrade`
- `mmoitems gem leveling`
- `duplicate gemstone fusion`
- `paper rpg plugin`
- `mythiclib addon`
- `minecraft rpg gemstones`
- `gem progression system`
- `drag and drop gem upgrade`

Suggested BuiltByBit tags:

`mmoitems`, `gemstone`, `gem upgrade`, `gem fusion`, `sockets`, `rpg`, `paper`, `mythiclib`, `drag and drop`, `addon`

## Feature Phrases

Use these phrases naturally across the title, short description, feature list, and FAQ:

- Upgrade exact duplicate gems inside the existing socket.
- Match both gem id and level before merging.
- Keep different gem ids and levels out of the upgrade flow.
- Configure a global max level with per-gem overrides.
- Map every level to a custom MMOItems item id.
- Preserve MMOItems native handling for non-upgrade cases.
- Do not consume the cursor gem when the next template is missing or invalid.

## File Naming

Release file:

`OmniGemLevel-1.0.0.jar`

Future releases:

`OmniGemLevel-{version}.jar`

Marketplace assets:

- `omnigemlevel-resource-banner-1920x640.png`
- `omnigemlevel-01-product-cover-1920x1080.png`
- `omnigemlevel-02-gem-fusion-1920x1080.png`
- `omnigemlevel-03-safe-matching-1920x1080.png`
- `omnigemlevel-04-flexible-config-1920x1080.png`
- `omnigemlevel-05-mmoitems-workflow-1920x1080.png`

## Image Alt Text

1. `OmniGemLevel by SalyVn with five gemstone levels`
2. `MMOItems duplicate gemstones upgrading from level one to level two`
3. `Existing MMOItems gem socket upgraded without using another socket`
4. `Safe matching rules for gem id, gem level, and max level`
5. `Global max level and per-gem override configuration`
6. `MMOItems GEM_ID_LEVEL setup and drag-and-drop upgrade workflow`

## Positioning Notes

- Lead with the player benefit: duplicate gems become progression without wasting sockets.
- Show the exact-pair rule immediately after the first showcase image.
- Mention `default-max-level: 5` because it is concrete and easy to scan.
- Keep requirements visible before the final documentation link.
- Do not advertise Folia or Minecraft versions other than 1.21 until they are tested.
- Do not claim a success-rate system; exact-pair upgrade is guaranteed only when the next template is valid.

## Suggested Search Title And Meta

Page title:

`OmniGemLevel - MMOItems Gem Upgrade Addon | SalyVn`

Meta description:

`OmniGemLevel adds same-id, same-level gemstone fusion to MMOItems. Upgrade existing sockets, set global or per-gem caps, and preserve native behavior.`

## Publish Checklist

- Upload the release as `OmniGemLevel-1.0.0.jar`.
- Select Paper and Minecraft 1.21 only.
- Add MMOItems and MythicLib as required dependencies.
- Paste `builtbybit-description.bbcode` while the editor is in BBCode mode.
- Verify all six hosted images render before publishing.
- Add the documentation URL as the primary external link.
- Keep the first 160 characters focused on `MMOItems gem upgrade` intent.

## Unresolved Questions

- Pricing and license type were not provided.
- Support channel and support duration were not provided.
- Additional Minecraft versions have not been verified.
